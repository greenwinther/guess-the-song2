{
  "name": "guess-the-song2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace apps/backend run dev",
    "build": "npm --workspace apps/backend run build",
    "start": "npm --workspace apps/backend run start",
    "drizzle:generate": "npm --workspace apps/backend run drizzle:generate",
    "drizzle:push": "npm --workspace apps/backend run drizzle:push"
  }
}
